# Copy to tools/release/.release.env and adjust values for your machine.

# HAR output path (repo-relative or absolute)
HAR_PATH=packages/streaming-markdown/build/default/outputs/default/streaming_markdown.har

# Optional: verify-release.sh defaults
PACKAGE_FILE=packages/streaming-markdown/oh-package.json5
# RELEASE_TAG=v2.1.0

# Optional: override OHPM config file path (otherwise use ~/.ohpm/.ohpmrc)
# OHPM_RC_PATH=/Users/you/.ohpm/.ohpmrc
